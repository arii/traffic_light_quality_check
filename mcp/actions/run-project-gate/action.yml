name: "Run Project Gate"
description: "Abstracted logic for building and testing monorepo sub-projects"
inputs:
  target:
    description: "Target project to validate (e.g., 'root', 'boomtick-mcp')"
    required: true
  github_token:
    description: "GitHub token for API access (e.g., ratchet-any)"
    required: false

runs:
  using: "composite"
  steps:
    - name: Resolve Project Paths
      id: resolve-paths
      shell: bash
      env:
        TARGET: ${{ inputs.target }}
      run: |
        # Use centralized script for CLI path resolution
        if [ -f "scripts/resolve-cli.sh" ]; then
          CLI_PATH=$(bash scripts/resolve-cli.sh | sed "s|$(pwd)/||")
        elif [ -f "boomtick-pkg/scripts/resolve-cli.sh" ]; then
          CLI_PATH=$(bash boomtick-pkg/scripts/resolve-cli.sh | sed "s|$(pwd)/||")
        elif [ -f "boomtick/scripts/resolve-cli.sh" ]; then
          CLI_PATH=$(bash boomtick/scripts/resolve-cli.sh | sed "s|$(pwd)/||")
        elif [ -d "boomtick-pkg/cli" ]; then
          CLI_PATH="boomtick-pkg/cli"
        elif [ -d "boomtick/cli" ]; then
          CLI_PATH="boomtick/cli"
        elif [ -d "cli" ]; then
          CLI_PATH="cli"
        else
          CLI_PATH="cli"
        fi
        echo "CLI_PATH=$CLI_PATH" >> "$GITHUB_OUTPUT"

        case "$TARGET" in
          root)
            echo "FILTER_PATH=." >> "$GITHUB_OUTPUT"
            ;;
          boomtick-mcp)
            if [ -d "mcp" ]; then
              echo "FILTER_PATH=./mcp" >> "$GITHUB_OUTPUT"
            else
              echo "FILTER_PATH=./mcp" >> "$GITHUB_OUTPUT"
            fi
            ;;
          *)
            echo "::error::Invalid target: $TARGET. Must be 'root' or 'boomtick-mcp'."
            exit 1
            ;;
        esac

    - name: Validate Runtime
      if: inputs.target == 'root'
      run: |
        echo "::group::Validate Runtime"
        trap 'echo "::endgroup::"' EXIT
        pnpm run check:runtime-files
        pnpm run doctor
      shell: bash

    - name: pnpm install
      env:
        TARGET: ${{ inputs.target }}
        FILTER_PATH: ${{ steps.resolve-paths.outputs.FILTER_PATH }}
      run: |
        echo "::group::pnpm install"
        trap 'echo "::endgroup::"' EXIT
        if [ "$TARGET" = "root" ]; then
          pnpm install --frozen-lockfile --engine-strict=false
        else
          pnpm install --frozen-lockfile --filter "$FILTER_PATH" --engine-strict=false
        fi
      shell: bash

    - name: Verify lockfile integrity (root only)
      if: inputs.target == 'root'
      run: |
        if ! git diff --quiet pnpm-lock.yaml; then
          echo "::error::pnpm-lock.yaml is out of date. Run pnpm install and commit."
          exit 1
        fi
      shell: bash

    - name: Lint
      if: inputs.target == 'root'
      run: |
        pnpm run verify:schemas
        pnpm run lint
      shell: bash

    - name: Type-check / Build
      env:
        TARGET: ${{ inputs.target }}
        CLI_PATH: ${{ steps.resolve-paths.outputs.CLI_PATH }}
        FILTER_PATH: ${{ steps.resolve-paths.outputs.FILTER_PATH }}
      run: |
        if [ "$TARGET" = "boomtick-mcp" ]; then
          if command -v td-cli >/dev/null 2>&1; then
            PYTHONPATH="$CLI_PATH" td-cli schema --generate
          else
            PYTHONPATH="$CLI_PATH" python3 "$CLI_PATH/dev_tools/schema_gen.py"
          fi
          npx tsx "$FILTER_PATH/scripts/sync-contracts.ts"
          pnpm --filter "$FILTER_PATH" run sync:mcp-schemas
        fi

        if [ "$TARGET" = "root" ]; then
          pnpm run type-check
        else
          pnpm --filter "$FILTER_PATH" run build
        fi
      shell: bash

    - name: Python Linting
      if: inputs.target == 'root'
      continue-on-error: true
      run: |
        echo "::group::Python Linting"
        trap 'echo "::endgroup::"' EXIT
        if [ -f "requirements-dev.txt" ]; then
          pip install -r requirements-dev.txt
        fi
        pnpm run lint:python
        pnpm run type-check:python
      shell: bash

    - name: Run Unit Tests
      env:
        TARGET: ${{ inputs.target }}
        FILTER_PATH: ${{ steps.resolve-paths.outputs.FILTER_PATH }}
      run: |
        if [ "$TARGET" = "root" ]; then
          pnpm test
        else
          pnpm --filter "$FILTER_PATH" run test
        fi
      shell: bash

    - name: Run CLI Unit Tests
      if: inputs.target == 'root' || inputs.target == 'boomtick-mcp'
      env:
        CLI_PATH: ${{ steps.resolve-paths.outputs.CLI_PATH }}
      run: |
        echo "::group::Pytest"
        trap 'echo "::endgroup::"' EXIT
        if command -v td-cli >/dev/null 2>&1; then
          PYTHONPATH="$CLI_PATH" td-cli test cli
        else
          PYTHONPATH="$CLI_PATH:$CLI_PATH/dev_tools" python3 -m pytest "$CLI_PATH/tests/"
        fi
      shell: bash

    - name: Check for dead code
      if: inputs.target == 'root'
      run: pnpm run knip
      shell: bash

    - name: TypeScript 'any' Ratchet
      if: inputs.target == 'root'
      run: |
        echo "::group::Ratchet Any"
        trap 'echo "::endgroup::"' EXIT
        export PYTHONPATH="${{ steps.resolve-paths.outputs.CLI_PATH }}:${{ steps.resolve-paths.outputs.CLI_PATH }}/dev_tools"
        export PATH="$HOME/.local/bin:/github/home/.local/bin:$PATH"
        td-cli gh ratchet-any
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
      shell: bash
