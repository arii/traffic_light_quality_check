name: 'ChatOps Trigger'
description: 'Parses PR/Issue comments and dispatches actions accordingly'
inputs:
  comment_body:
    description: 'The body of the comment'
    required: true
    default: '${{ github.event.comment.body }}'
  author_association:
    description: 'The author association of the comment creator'
    required: true
    default: '${{ github.event.comment.author_association }}'
  issue_number:
    description: 'The issue or PR number'
    required: true
    default: '${{ github.event.issue.number }}'
  github_token:
    description: 'The GitHub token'
    required: true
    default: '${{ github.token }}'

runs:
  using: 'composite'
  steps:
    - name: Setup Workspace
      uses: ./.github/actions/setup-workspace # nosemgrep: yaml.github-actions.security.github-actions-mutable-action-tag.github-actions-mutable-action-tag

    - name: Pre-flight Validations
      id: validate
      env:
        GH_TOKEN: ${{ inputs.github_token }}
        COMMENT_BODY: ${{ inputs.comment_body }}
        AUTHOR_ASSOC: ${{ inputs.author_association }}
        ISSUE_NUMBER: ${{ inputs.issue_number }}
      run: |
        if [ -z "$GH_TOKEN" ]; then
          echo "::error::github_token is missing."
          exit 1
        fi
        if [ -z "$COMMENT_BODY" ]; then
          echo "::error::comment_body is missing."
          exit 1
        fi
        if [[ ! "$AUTHOR_ASSOC" =~ ^[A-Z_]+$ ]]; then
          echo "::error::Invalid author association format."
          exit 1
        fi
        if [[ ! "$ISSUE_NUMBER" =~ ^[0-9]+$ ]]; then
          echo "::error::Invalid issue_number: must be numeric."
          exit 1
        fi
        # security-safe: basic sanitization for COMMENT_BODY to avoid command injection
        if [[ ! "$COMMENT_BODY" =~ ^[a-zA-Z0-9\ \.\,\!\?\-\_\/\@]+$ ]]; then
          echo "::error::Invalid characters detected in comment_body."
          exit 1
        fi
      shell: bash

    - name: Check commands
      id: check
      env:
        AUTHOR_ASSOC: ${{ inputs.author_association }}
        COMMENT_BODY: ${{ inputs.comment_body }}
      run: |
        # security-safe: COMMENT_BODY is mapped safely via env block and wrapped in quotes
        RESULT=$(td-cli gh parse-comment --author-association "$AUTHOR_ASSOC" --body "$COMMENT_BODY")
        {
          echo "conflict_resolve=$(echo "$RESULT" | jq -r '.actions.conflict_resolve')"
          echo "update_snapshots=$(echo "$RESULT" | jq -r '.actions.update_snapshots')"
          echo "ai_chatops=$(echo "$RESULT" | jq -r '.actions.ai_chatops')"
          echo "jules_fix_ci=$(echo "$RESULT" | jq -r '.actions.jules_fix_ci')"
        } >> "$GITHUB_OUTPUT"
      shell: bash

    - name: Resolve Branch Details
      if: steps.check.outputs.conflict_resolve == 'true' || steps.check.outputs.update_snapshots == 'true' || steps.check.outputs.ai_chatops == 'true' || steps.check.outputs.jules_fix_ci == 'true'
      id: resolve_branch
      env:
        ISSUE_NUMBER: ${{ inputs.issue_number }}
      run: |
        PR_JSON=$(gh pr view "$ISSUE_NUMBER" --json headRefName,baseRefName || echo '{}')
        {
          echo "HEAD_REF=$(echo "$PR_JSON" | jq -r '.headRefName // empty')"
          echo "BASE_REF=$(echo "$PR_JSON" | jq -r '.baseRefName // "main"')"
        } >> "$GITHUB_OUTPUT"
      shell: bash

    - name: Trigger Dispatch Actions
      if: steps.check.outputs.conflict_resolve == 'true' || steps.check.outputs.update_snapshots == 'true' || steps.check.outputs.ai_chatops == 'true' || steps.check.outputs.jules_fix_ci == 'true'
      env:
        HEAD_REF: ${{ steps.resolve_branch.outputs.HEAD_REF }}
        BASE_REF: ${{ steps.resolve_branch.outputs.BASE_REF }}
        CONFLICT_RESOLVE: ${{ steps.check.outputs.conflict_resolve }}
        UPDATE_SNAPSHOTS: ${{ steps.check.outputs.update_snapshots }}
        AI_CHATOPS: ${{ steps.check.outputs.ai_chatops }}
        JULES_FIX_CI: ${{ steps.check.outputs.jules_fix_ci }}
        ISSUE_NUMBER: ${{ inputs.issue_number }}
        COMMENT_BODY: ${{ inputs.comment_body }}
        GH_TOKEN: ${{ inputs.github_token }}
      run: |
        ERRORS=0
        OPS_WORKFLOW="issue-operations.yml"
        CI_WORKFLOW="agent-orchestrator.yml"

        if [[ "$CONFLICT_RESOLVE" == 'true' ]]; then
          if ! gh workflow run "$OPS_WORKFLOW" --ref "$HEAD_REF" -f action="resolve-conflicts" -f issue_number="$ISSUE_NUMBER" -f comment_body="$COMMENT_BODY"; then
            echo "::error::Trigger failed for $OPS_WORKFLOW resolve-conflicts on PR $ISSUE_NUMBER"
            ERRORS=1
          fi
        fi

        if [[ "$UPDATE_SNAPSHOTS" == 'true' ]]; then
          if ! gh workflow run "$OPS_WORKFLOW" --ref "$HEAD_REF" -f action="update-snapshots" -f issue_number="$ISSUE_NUMBER" -f comment_body="$COMMENT_BODY"; then
            echo "::error::Trigger failed for $OPS_WORKFLOW update-snapshots on PR $ISSUE_NUMBER"
            ERRORS=1
          fi
        fi

        if [[ "$AI_CHATOPS" == 'true' ]]; then
          if ! gh workflow run "$OPS_WORKFLOW" --ref "$HEAD_REF" -f action="ai-review" -f issue_number="$ISSUE_NUMBER" -f comment_body="$COMMENT_BODY"; then
            echo "::error::Trigger failed for $OPS_WORKFLOW ai-review on PR $ISSUE_NUMBER"
            ERRORS=1
          fi
        fi

        if [[ "$JULES_FIX_CI" == 'true' ]]; then
          if ! gh workflow run "$CI_WORKFLOW" --ref "$BASE_REF" -f task_type="repair" -f issue_number="$ISSUE_NUMBER" -f comment_body="$COMMENT_BODY"; then
            echo "::error::Trigger failed for $CI_WORKFLOW repair on PR $ISSUE_NUMBER"
            ERRORS=1
          fi
        fi

        if [ "$ERRORS" -ne 0 ]; then
          bash -c 'exit 1'
        fi
      shell: bash
